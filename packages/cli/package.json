{
  "name": "@tufte/chart-cli",
  "version": "0.2.0",
  "description": "Render `chart` fenced blocks to ASCII — pre-render markdown in place, or pipe a spec via stdin.",
  "type": "module",
  "bin": {
    "chart": "bin/chart.js"
  },
  "files": [
    "bin"
  ],
  "keywords": [
    "ascii",
    "chart",
    "tufte",
    "markdown",
    "cli",
    "pre-render"
  ],
  "scripts": {
    "test": "vitest run"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/arvindang/tufte.git",
    "directory": "packages/cli"
  },
  "homepage": "https://tufte.ai",
  "bugs": "https://github.com/arvindang/tufte/issues",
  "author": "Arvin Dang",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@tufte/chart-core": "0.2.0"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}
