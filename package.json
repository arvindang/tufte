{
  "name": "tufte-monorepo",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "packages/*",
    "app"
  ],
  "scripts": {
    "test": "npm test --workspaces --if-present",
    "sync:obsidian-manifest": "node scripts/sync-obsidian-manifest.mjs",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "npm test --workspaces --if-present && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.0"
  }
}
